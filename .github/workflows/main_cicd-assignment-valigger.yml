# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy Node.js app to Azure Web App

on:
  push:
    branches:
      - master
  workflow_dispatch:
 
env:
  DATABASE_URL: ${{ vars.DATABASE_URL }}
  PORT: ${{ vars.PORT }}
 
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      id-token: write # Required for requesting the JWT
      contents: read #This is required for actions/checkout
 
    steps:
      - uses: actions/checkout@v4
 
      - name: Set up Node.js version
        uses: actions/setup-node@v3
        with:
          node-version: '24.x'
 
      - name: npm install project dependencies
        run: npm install
 
      - name: Install playwright browsers
        run: npx playwright install
 
      - name: build, and test
        run: |
          npm run build --if-present
          npm run test --if-present
 
      - name: Remove node_modules to reduce deployment package size
        run: rm -rf node_modules        
 
      # - name: Upload test artifacts
      #   if: failure()
      #   uses: actions/upload-artifact@v4
      #   with:
      #     name: playwright-traces
      #     path: test-results
 
      - name: Login to Azure
        # if: always()
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_684AC64F4F6D47159DB9EDA305060EB3 }}
          tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_D74FF5E286C042DA8F15D6F6B9E60EF7 }}
          subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_898407468C1A4186952A3219B59CF75A }}

      - name: 'Deploy to Azure Web App'
        # if: always()
        id: deploy-to-webapp
        uses: azure/webapps-deploy@v3
        with:
          app-name: 'valiggers'
          slot-name: 'Production'
          package: .
          startup-command: 'npm install && node ./src/server.js'
